# QA Report

## V2 本轮后端验证（2026-09-24）

执行 `PYTHONPATH=backend .venv/bin/python -m unittest discover -s backend/tests`：121/121 通过；`.venv/bin/python -m unittest discover -s tests`：启动器 4/4 通过。自动后端测试使用临时数据、假凭据/假 provider 或 ffmpeg 合成素材。另执行了一次独立真实接口冒烟，见下方记录。

新增 `backend/tests/test_v2_workflow.py` 覆盖真实 HTTP、SQLite 事务及文件副作用：草稿 JSON 字段持久化、revision 冲突；四个并发重复请求只入队一组；编译后字数与 MP3 参数预检；确认后目录变化要求重新预检；固定音色编号不能静默重排；一次性授权和双任务共享租约；排队取消不执行 worker；WAV/M4A 裁剪与持久音色；作品库完整 DTO 和 20/50 分页；音频/报告回收恢复且不覆盖同名文件；最终版本及 journal 恢复；媒体符号链接替换和外站写操作拦截。模板独立回归覆盖 42 个种子、变量安全展开、收藏与内置只读。

原生文件夹选择器已通过 Cocoa 编译，已实际运行并返回合法取消结果。API 选择中文目录、取消、目录读写由可控 picker 验证。桌面自动化读取原生窗口超时，自动点击“选择此文件夹”的整条系统窗口路径未取得证据；该限制不记为通过。

隔离浏览器服务为 `backend/qa_app.py`：仅允许专用临时目录，默认 8766，包含六条合成测试信号及失败/中断记录，使用假钥匙串和假 provider。它可验证真实页面的生成、轮询、试听、下载、音色与历史操作。合成音频不代表 Next 效果，不能作为真实模型听感证据。

前端 Vitest 4.1.11 与浏览器 Playwright 的最终数量见 [逐项验收清单](docs/V2-验收清单.md)。生产构建通过。流程覆盖自动保存、模板变量与撤销、设置默认值、作品库操作与两种回收恢复、双候选、历史快照续作与筛选保留、参考导入裁剪/持久化/刷新后确认。测试依赖升级后的 npm audit 为 0 项漏洞。

追加验收：删去创作台整个模式说明区，主界面无“新建项目”；顶栏直接切换明暗并持久化，刷新保持；1280×720时页面与声音/输出侧栏均无纵向溢出，高级参数弹窗也无需滚动。Finder入口发出对应已登记目录的POST请求。弹窗连续Tab键焦点不逸出，Esc关闭后回到原按钮。

四种视口 1440×900、1280×720、768×1024、390×844 的创作、作品库、模板、设置页均无整页横向/纵向溢出；深色主题另检查 1440×900 的四页。主要工具位于一屏，长脚本与列表在自身区域滚动。Webfont 检查为真实加载成功。界面截图 DPR=2，保存在 `docs/screenshots/v2/`；素材、路径与凭据已脱敏。

内置浏览器对隔离端口返回 `net::ERR_BLOCKED_BY_CLIENT`，因此浏览器测试回退到 Playwright Chromium。页面标题、非空内容、无框架错误覆盖层、控制台与真实交互分别检查。结果页独立 Chrome 验证播放/暂停、拖动、回退10秒、音量、选区循环、跨项目 A/B 与路由释放；最终 A/B 位置差约0.9毫秒，单路可听，控制台无错误。此时间差是本机一次实测值，不是所有设备的保证。

独立复查后新增单实例所有权测试，第二进程不能中断第一进程的任务/释放引用；关闭或初始化失败后释放锁。回收目标目录发生替换时拒绝移动，保留源文件与记录。历史续作恢复当次文案、参数、引用绑定及输出目录；不会覆盖旧草稿。

### V2 真实 API 冒烟

明确执行 `backend/live_smoke.py --confirm-one-call`，共1次付费模型调用，无参考音频。Next 返回2.193286秒、48kHz、双声道MP3，ffprobe与ffmpeg完整解码均通过。测试用单独临时数据目录，不修改正式作品。未进行 ASR 台词完整度比对或声纹相似度评分，文件通过不推导听感通过。

## 以下为 V1 历史记录（不作为 V2 验收结果）

验证日期：2026-09-23

## 自动化回归

- Backend：26/26 通过。
- Frontend：26/26 通过。
- macOS launcher：4/4 通过。
- Vite production build：通过。
- macOS 应用包：arm64 Mach-O 启动器、Info.plist、项目路径解析和 Finder 双击链路通过。
- 一键启动后 `/api/health` 返回模型与本地 Host；停止脚本执行后端口关闭。
- Security.framework：临时钥匙串项写入、读取、删除闭环通过。

## 浏览器端到端

- 首屏标题、主导航、结构化编辑器与所有参数控件可见。
- 灵感模板、Prompt 标签、格式、采样率、声道、AIGC 与 MP3 参数交互通过。
- 2 秒 WAV 参考音频本地上传预检通过，页面正确显示时长、大小和编码。
- 项目、历史、音色参考、设置与结果路由均通过。
- 播放、暂停、后退、音量、下载、真实版本切换、A/B 同步播放和最终版本标记通过。
- 直接刷新子路由后的 CSRF 初始化、参考音频删除、同项目续作和 URL 指定版本优先级通过。
- WAV/MP3 使用浏览器解码后的真实峰值波形；PCM 明确提供下载并禁用网页伪播放。
- 7 个创作模式均联动专属说明、模板与起始 Prompt；手写 Prompt 在切换模式时保持不变。
- 控制台在最终回归中无 error/warn。

## 真实 API 冒烟

- 模型：`qwen-audio-3.1-tts-next`
- 场景：无参考音频的中文旁白。
- 生成耗时：26.4 秒。
- 成片：6.56 秒、314,924 字节、24kHz、单声道 WAV。
- 页面实际播放：readyState 4，播放进度正常增长。
- ffprobe：通过。
- ffmpeg 完整解码：通过。
- 凭据仅存储于 macOS 钥匙串，截图、报告和源码未记录凭据值。

## 视觉对照

与两张 1586×992 概念图逐项对照：

1. 信息架构一致：左侧导航、顶栏、中央编辑/结果区、右侧检查器与底部命令条完整保留。
2. 视觉系统一致：午夜蓝背景、石墨面板、细描边、电光蓝主操作、青色波形和绿色成功态。
3. 创作台密度一致：7 模式横排、5 类标签、行号编辑器、3 张灵感模板与参数侧栏均在首屏可见。
4. 结果台层级一致：版本切换、大波形、结构时间线、A/B 对比和右侧验收报告清晰分区。
5. 数据诚实性增强：只显示真实生成版本；只有两条以上结果时显示 A/B；Prompt 时间线明确标注为结构顺序。
6. 窄屏适配：390×844 下改用底部导航，命令条不遮挡主要编辑区，播放器与下载操作可用。

截图位于 `docs/screenshots/`。
